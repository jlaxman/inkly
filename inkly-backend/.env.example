# Application
NODE_ENV=development
PORT=3001

# Database (configured in docker-compose.yml for Docker)
# For local development, uncomment and configure:
# DATABASE_URL="postgresql://user:password@localhost:5432/inkly?schema=public"

# JWT
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRES_IN=7d

# Redis (configured in docker-compose.yml for Docker)
# For local development, uncomment and configure:
# REDIS_HOST=localhost
# REDIS_PORT=6379

# Email (Optional - for production)
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=your-email@example.com
# SMTP_PASS=your-password
# SMTP_FROM=noreply@inkly.com

# AWS Configuration (Optional - for production)
# AWS_REGION=us-east-1
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_S3_BUCKET=inkly-media

# AWS Cognito (Optional - for production)
# COGNITO_USER_POOL_ID=
# COGNITO_CLIENT_ID=

# LLM API Keys (Optional)
# OPENAI_API_KEY=
# ANTHROPIC_API_KEY=

# Stripe (Optional - for payments)
# STRIPE_SECRET_KEY=
# STRIPE_PUBLISHABLE_KEY=
