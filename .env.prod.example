# Production environment
# Copy to .env.prod and set values. Do not commit .env.prod.
# For public URLs see docs/PUBLIC_URLS.md (www.inkly.co.in, gamma.inkly.co.in).

# Required
POSTGRES_USER=inkly_user
POSTGRES_PASSWORD=change_me_secure_password
POSTGRES_DB=inkly_prod
DATABASE_URL=postgresql://inkly_user:change_me_secure_password@postgres:5432/inkly_prod?schema=public
JWT_SECRET=change_me_use_openssl_rand_hex_32

# Public URLs — set per environment (gamma vs prod)
# Gamma:  FRONTEND_URL=https://gamma.inkly.co.in   NEXT_PUBLIC_API_URL=https://gamma.inkly.co.in/api
# Prod:   FRONTEND_URL=https://www.inkly.co.in     NEXT_PUBLIC_API_URL=https://www.inkly.co.in/api
FRONTEND_URL=https://www.inkly.co.in
NEXT_PUBLIC_API_URL=https://www.inkly.co.in/api

# Optional (defaults used if unset)
JWT_EXPIRES_IN=7d

# Cloudflare Tunnel (gamma / prod). Required for deploy when using Cloudflare.
# Get token: Zero Trust → Tunnels → Create tunnel → copy token (see docs/CLOUDFLARE_SETUP.md).
CLOUDFLARE_TUNNEL_TOKEN=

# Optional – SMTP. If omitted, emails are skipped (welcome/order emails disabled).
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=noreply@yourdomain.com
